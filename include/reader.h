#ifndef READER_H
#define READER_H

#include <string>
#include <fstream>
#include <map>

class reader{
private:
    std::ifstream read;
public:
    std::string read(int id, std::string id);
};

#endif // READER_H
